# Reference only — `wrangler dev` reads .dev.vars, not this file.
# Setup:  cp .dev.vars.example .dev.vars  && fill in RPC keys.
#
# Production secrets/vars live in the Cloudflare dashboard (or `wrangler secret put`).
# Do not commit real keys or production URLs here.

PORT=9000
